# Booking remediation — release gates

Prepared against upstream `197030a1a9d5404740a0a52c378f83dd5da8f900`; evidence updated 2026-09-24. These changes have **not been deployed to production**. Both SQL stages are applied in isolated test project `rktbdjhaqgzmvzsyracf`, with synthetic data only. No production data was modified for implementation tests and no real emails were sent.

## What changes

- Every protected server action verifies an active account and its individual permission before using the backend client. The final database stage denies direct anonymous/authenticated table and RPC access. Existing menu permissions remain the source of access decisions.
- Booking creation, status changes and date cancellation run in database transactions with a shared date lock. Confirmation numbers use a unique sequence. Cancellation is terminal for a reservation. Historical rows and existing confirmation numbers remain intact.
- Email events are created with the transaction, preserving recipient and reservation details. Sending uses a lease, frozen payload and provider idempotency key. A receipt failure is recoverable; uncertain attempts older than 23 hours require manual provider reconciliation. Legacy messages are not blindly retried.
- Users are linked to their stable Auth identity. Deactivation preserves history. The interface handles missing permissions, stale calendar responses and paginated reservation/email lists. Type errors now fail the build.

## Local evidence and limits

`pnpm test`: 18 passing tests, including denial before database access for all protected server actions, role checks, capacity, rollback on an injected outbox failure, consistent date cancellation, confirmation uniqueness, immutable event cycles, exclusive email leases, mocked provider receipt failure, HTML escaping, and authenticated cron execution. TypeScript checks pass. The earlier full local build compiled all 20 routes; the latest local build attempt could not fetch Google Fonts in the restricted environment. Use the successful Vercel build on the final commit as current build evidence.

The encrypted application backup was restored to a disposable local PostgreSQL-compatible database, then both SQL stages were applied. Every original value and record remained unchanged: 2 tours, 9 users, 114 dates, 11 reservations, 28 emails, 84 permissions, 2 minimum-participant alerts, 0 audit entries. The backup covers these application tables and their schema/permissions. It is **not** a full Supabase project backup and does not include Auth passwords. It and its recovery key are stored privately outside this repository.

Local PGlite tests use one database connection and **do not establish concurrent-session safety or production load performance**. All four real-connection tests in `tests/postgres_concurrency.py` passed on PostgreSQL 17.11 in [GitHub run 36043935242](https://github.com/gmnatany-sys/Guides-App/actions/runs/36043935242) on 2026-09-24. They accept only an empty loopback database named `guides_remediation_test`. Recheck the workflow result on the final commit before release. These tests do not establish production load performance, browser behavior or hosted Auth integration.

## Before any production change

1. Complete and verify [draft PR #3](https://github.com/gmnatany-sys/Guides-App/pull/3), on branch `fix/booking-safety`, against the tested local package and current remote head. Do not overwrite intervening changes. The browser upload is performed in batches; intermediate preview build failures do not qualify the final package for release. Require a successful build on the final commit.
2. Run the PostgreSQL concurrency workflow. Create a preview linked to a separate test Supabase project with synthetic users and reservations. Never attach this preview to the production database or give it a real `RESEND_API_KEY`.
3. CLI generation is complete via Supabase CLI 2.117.0 in [GitHub run 36049832848](https://github.com/gmnatany-sys/Guides-App/actions/runs/36049832848). The generated files are `supabase/migrations/20260924194130_booking_prepare.sql` and `20260924194133_booking_lockdown.sql`; CI verifies their bytes against the reviewed SQL drafts. Their SHA256 values are respectively `14341c9ef8e82c09a9cb927367fef8f420a801e5d17576dc464661e952991b35` and `c036db5bb38b8f04c18b187c88c9c59dab9e080da1f77c97e9454e4b3c604754`. The test project's equivalent stages were applied with hosted versions `20260924191121` and `20260924191136`. Do not apply duplicate stages there. Apply production stages separately and register each with its CLI-generated version only after successful execution; do not run a push that applies both stages before the app is ready.
4. Hosted test evidence covers admin, operation, agent, supplier, disabled user, no-page-permission user, and signed-out sessions; creation, confirmation, rejection, cancellation through reservation, availability, minimum-participant and supplier interfaces; preserved history; reservation pagination beyond 100 and email pagination beyond 50. Anonymous Data API calls return permission denied after lockdown. The provider key is intentionally invalid: delivery failures are recorded and zero messages are marked SENT. This proves failure handling, not actual provider delivery. Inspect the latest UI acceptance record and require final build/CI success.
5. Recheck current production constraints with read-only queries: duplicate lowercased emails or confirmation numbers, Auth links for the users who must log in, invalid participant counts, overcapacity, and active bookings on cancelled dates. Stop for investigation instead of rewriting records automatically. Compare permissions to the approved access model.
6. Take a fresh recoverable backup immediately before cutover. Managed production daily backups were verified in the Supabase dashboard (latest observed: 2026-09-24 19:33:24 UTC). The application-data restore test is not a full managed project/Auth restore test. Do not rely on this task's earlier snapshot for activity that occurred afterward.

One production access decision remains: an active agent with eight historical reservations has no matching Auth account. Establish whether the person needs their own login or is only booked on behalf of others. Preserve the profile and reservation links; do not silently create or invite a new account.

Hosted scheduler acceptance: SQL alert checks ran twice at 19:45/19:46 UTC, created the expected 7/5/3-day events, and did not duplicate them. On application commit `b696fd0`, the HTTP cron ran at 20:11/20:12 UTC. The first returned JSON HTTP 500 with 13 failed deliveries and zero sent because of the invalid test mail key. The second returned JSON HTTP 200 with zero eligible deliveries during the retry delay; this is not proof of delivered mail or an empty outbox. Missing application authorization returned 401. Both test jobs were deactivated, and the approved temporary Vercel bypass was revoked; a subsequent request using it reached the Vercel login page instead of application JSON.

Managed-extension caveat: Supabase owns `pg_net` under `supabase_admin`; project-role REVOKE statements returned without changing its default client grants. Do not report these grants as revoked. The `net` schema is not exposed through the Data API (verified HTTP 406 / PGRST106); `cron` and Vault also stay unexposed, and public application RPCs deny client access. Never expose these schemas or add a client-callable SQL proxy. The advisor's non-relocatable-extension warning is documented in [Supabase pg_net guidance](https://supabase.com/docs/guides/database/extensions/pg_net). Recheck effective privileges and API schema exposure if changing extension versions or configuration.

## Controlled cutover

1. Choose a controlled period for switching writes. Keep old and new booking writers from running concurrently: the old application does not participate in the new database locks. Let outstanding old requests finish. Do not advertise uninterrupted operation without this coordination.
2. Apply only the additive prepare migration. No table resets, row deletion, confirmation renumbering or replacement of business data is involved. Check lock duration and migration success; verify every existing user who needs access has the expected Auth mapping. Do not run live booking RPCs as probes.
3. Build and deploy the tested server-guarded application using production environment variables. Never promote the isolated preview artifact: it contains staging public variables. Verify `APP_SUPABASE_URL`, `JWT_8`, `APP_SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY` and `CRON_SECRET` in the correct environment without exposing their values. Confirm production login/read flows and server permissions before revoking old grants. Reload old browser tabs onto the new application.
4. Apply the lockdown migration. Run `database/verify-remediation.sql`; it must return zero findings. Verify signed-out and direct client access are denied and authorized pages still work. Enable normal writes through the verified new version.
5. Configure a production Supabase schedule calling `/api/cron/check-minimum-participants` using a production CRON_SECRET held in Vault. The isolated test script `database/staging-scheduler.sql` is deliberately restricted to the synthetic test project and creates an inactive job; do not copy its preview URL or bypass credential into production. Test scheduling uses pg_cron and pg_net, not a Vercel Hobby high-frequency cron. A 5-minute starting interval is below the 23-hour uncertain-delivery reconciliation window. Verify both cron execution and the HTTP JSON result; an enqueued request or Vercel login HTML is not application success. Inspect the outbox backlog because processing is bounded to 20 per invocation. Immediate cancellation delivery is also queued after the response. Leave test schedules disabled and revoke temporary preview bypass access after testing.
6. Review the two historical alert emails older than 24 hours against provider delivery records before deciding whether either should be sent. There is intentionally no blanket resend or status reset.

## Stop and recovery

Stop promotion if the concurrency, preview, Auth, permission or scheduler gates fail. Keep the prior deployment available until the new application passes. After lockdown, the old application is not a compatible rollback: it uses revoked client grants. Roll back only to a tested server-guarded version compatible with the additive schema, or forward-fix the new version while writes are controlled. Do not restore broad public access as a rollback and never replace the live database with an older snapshot that would erase new orders. Additive columns and functions can remain in place.

The historical root-level permission export documents the pre-security state only. Do not replay it after lockdown. Minimum participants remains 4; the existing 7/5/3-day stages and server UTC date behavior are retained. Changing past-date rules, tour thresholds or viewing scope requires a separate business decision.

References: [PostgreSQL locks](https://www.postgresql.org/docs/current/explicit-locking.html), [Supabase permissions](https://supabase.com/docs/guides/database/postgres/row-level-security), [Resend idempotency](https://resend.com/docs/dashboard/emails/idempotency-keys), [GitHub PostgreSQL test services](https://docs.github.com/en/actions/tutorials/use-containerized-services/create-postgresql-service-containers).
